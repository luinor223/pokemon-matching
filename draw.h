

// NOTE
// mode = 0, draw normally
// mode = 1, highlight the cell
void drawCell(string text, int x, int y, int cellSizeRow, int cellSizeColumn, int bg_color = white, int text_color = black, int mode = 0); 

void drawLine(int x1, int y1, int x2, int y2, int mode);
